//
//  KeepSimpleCodeToRemember.swift
//  SwiftCoding
//
//  Created by Hitesh Kumar on 07/05/18.
//  Copyright © 2018 Hitesh Kumar. All rights reserved.
//

import Foundation

    /*

    // String Index
    func stringIndex(str: String, index: Int) -> String.Index {
        return str.index(str.startIndex, offsetBy: index)
    }

    // Array Sum
    let arr = [Int]()
    let sum = arr.reduce(0, +)
 
    // Long Ing
    var sum:Int64 = 0
    let arr:[Int64] = [1, 2, 3, 4, 5]
 
    // Initilize array with initial zero
    Array(repeating: -1, count: 100)
 
    // paas reference to edit the variable
    1. function parameter
        func getSum(value: inout Int)
        func getSumArray(arr: inout [Int])
    2. paas with address
        getSum(&value)
        getSum(&arr)
 
    // Set
    1. Define set
        var charSet = Set<Character>() // Set of characters
    2. Find if it have value in set or not
        charSet.contains(char)
    3. Insert value in set
        charSet.insert(char)

    // Define dictionary
    var charCount = [Character: Int]()
 
    // Tree Node
     class Node<T> {
         var value: T
         var left: Node?
         var right: Node?
 
         init(value: T) {
            self.value = value
        }
    }
 
    // 
 
 
    */


